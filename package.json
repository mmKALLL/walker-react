{
  "name": "walker-react",
  "version": "1.0.0",
  "private": true,

  "eslintConfig": {
    "env": {
      "browser": true
    },

    "globals": {
      "walker": true
    },

    "rules": {
      "semi": "off",
      "padded-blocks": "off"
    }
  }
}
